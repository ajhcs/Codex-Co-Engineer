interface:
  display_name: "Codex-Co-Engineer"
  short_description: "Delegate work to Grok, Cursor, and DSH"
  default_prompt: "Use $control-plumbob-agents to show status and delegate the requested work to the best peer agent."
dependencies:
  tools:
    - type: "mcp"
      value: "plumbob-harness-control"
      description: "ACP-first delegation and lifecycle control for Grok, Cursor Local, Cursor Cloud, and DSH"
policy:
  allow_implicit_invocation: true
