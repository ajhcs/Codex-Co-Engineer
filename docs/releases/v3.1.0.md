# Codex-Co-Engineer 3.1.0

Repository-side GitHub Release body for **Codex-Co-Engineer 3.1.0**.
This file is the publication source. It does not create a GitHub Release,
tag, or remote by itself.

**Date:** 2026-08-19
**Package / plugin / MCP id:** `codex-co-engineer`
**Skill:** `control-codex-co-engineer-agents`
**Tools:** `status`, `delegate`, `task`, `tasks`, `cancel`

## Highlights

Wait for delegated work until it reaches a terminal or needs-attention
state, without waking on routine text deltas. Deadlines are recorded with
a visible 20% margin. Diagnostics, deadline extension, and reply are
parameters on the existing five-tool catalog.

- **Recorded deadlines.** `delegate` records `expected_duration_ms`, a
  20% margin, and
  `deadline_at = created_at + ceil(expected_duration_ms * 1.20)`.
- **Terminal waits.** `task` `wait_until: "terminal"` waits for a
  terminal or needs-attention state. Disconnecting the waiter does not
  stop provider work.
- **Summary and diagnostics views** on `task`. Diagnostics are
  side-effect free, cursor-paged, byte-capped, and secret-redacted.
- **Same-session, exactly-once `task.reply`** for Grok and Cursor Local
  ACP sessions. DSH, Cursor Cloud, and CLI fallback report
  `same_session_reply_unsupported`.
- **Advertised 4-hour pending-call budget.** This is an advertised
  plugin budget, not a measured Codex Desktop hard limit.

**Limitation:** 5-minute, 30-minute, and 4-hour Codex Desktop
measurements were not executed in this worktree. If the host cuts the
call earlier, reconnect from `event_cursor`.

## Install a fresh checkout

Requires Node.js 24+, Git, and Codex CLI. Local providers also need
Linux `systemd --user`, `systemd-run` 244+, unified cgroup v2, and
`worktree-bootstrap` on `PATH`.

```bash
git clone https://github.com/ajhcs/Codex-Co-Engineer.git
cd Codex-Co-Engineer
codex plugin marketplace add "$PWD"
codex plugin add codex-co-engineer@codex-co-engineer
npm --prefix plugins/codex-co-engineer run setup
npm --prefix plugins/codex-co-engineer run setup:check
```

Start a new Codex session after installing the plugin. Authenticate
providers with their normal flows; setup does not log in for you:

```bash
grok login
cursor-agent login
plugins/codex-co-engineer/bin/set-model-api-key
```

## First run

1. Ask Codex: `Show Codex-Co-Engineer status.`
2. Local providers are ready only when `local_boundary.ready` is true.
3. Delegate a managed review, then wait:

```json
{
  "task_id": "review-auth-refactor",
  "provider": "grok",
  "repo": "/absolute/path/to/git-worktree",
  "role": "review",
  "workspace_mode": "managed",
  "prompt": "Review the current branch and report concrete correctness risks.",
  "expected_duration_ms": 600000
}
```

```json
{
  "task_id": "review-auth-refactor",
  "wait_until": "terminal"
}
```

Inspect the receipt before Codex pushes, opens a PR, or merges.

## Changelog

See [CHANGELOG.md](../../CHANGELOG.md) for the full 3.1.0 Added / Fixed /
Changed list.

## Maintainer publication (not performed here)

When a maintainer later publishes GitHub Releases, use this file as the
release body. Do not invent a tag or remote mutation from documentation
work. Suggested later command, not run by this change:

```bash
gh release create v3.1.0 --title "Codex-Co-Engineer 3.1.0" --notes-file docs/releases/v3.1.0.md
```
